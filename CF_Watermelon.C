#include <stdio.h>
int main ()
{
    int w;
    scanf("%d",&w);
    if(w==2 || w%2==1)
    {
        printf("No\n");
    }
    else
    {
        printf("Yes\n");
    }
    return 0;
}

